#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long n;
    do
    {
        n=get_long("Number: \n");
    }


    while(n!=0)
    {
        if(pos%2!=0)
        totalsum+= 2*(n%10);
        else

    }
}